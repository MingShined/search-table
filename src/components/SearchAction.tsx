import React, { Fragment } from 'react';
import { Button } from 'antd';
interface Props {}
const SearchAction: React.FC<Props> = props => {
  return (
    <Fragment>
      <Button>按钮</Button>
    </Fragment>
  );
};
export default SearchAction;
